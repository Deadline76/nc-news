

export const PostComment = ({articleId}) => {

    return (
        <div>
            <h3>Post a comment</h3>
            <input type="text" />
            <button>Submit</button>
        </div>
    )
}